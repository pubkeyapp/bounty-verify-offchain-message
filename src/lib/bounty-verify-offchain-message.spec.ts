import { bountyVerifyOffchainMessage } from './bounty-verify-offchain-message'

describe('bountyVerifyOffchainMessage', () => {
  it('should work', () => {
    expect(bountyVerifyOffchainMessage()).toEqual('bounty-verify-offchain-message')
  })
})
